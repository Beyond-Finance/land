version: 2
jobs:
  build:
    docker:
      - *default_docker_ruby_executor
    steps:
      - run:
          name: Placholder
          command: |
            echo 'Configuration file provided to appease CircleCI.'
            echo 'CircleCI not currently configured to do anything meaningful on this repo.'
  